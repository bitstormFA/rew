## dataframe umbrella — lazy relational DataFrame values and expression DSL.
##
## This is the public module for structured analytics scaffolding. It exposes
## backend-neutral DataFrame values, column expressions, and chainable relational
## verbs without choosing an execution engine.

import ./dataframe/core
export core
